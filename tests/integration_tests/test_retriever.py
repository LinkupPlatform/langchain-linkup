import warnings

# NOTE: there is no retriever integration in langchain-tests to this date (version 0.3.4), so we
# need to implement tests manually


def test_todo() -> None:
    # TODO: implement (empty test prevents test command from failing)
    warnings.warn("Test is not implemented")
